execution:
  - executor: selenium
    runner: testng
    testng-xml: testng-smoke.xml
    env:
      EXECUTION_MODE: remote
      ENVIRONMENT: qa
      BROWSER: chrome
    scenario: intralox-smoke

scenarios:
  intralox-smoke:
    script: blaze-test-package.jar
